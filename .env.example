# InvoiceFlow frontend (Vite)
VITE_API_BASE=

# Optional — override API proxy target in vercel.json if using a custom API host
# INVOICEFLOW_API_HOST=https://invoiceflow-ar-api.onrender.com

# Local API (services/api)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/invoiceflow
# JWT_SECRET=dev-secret-at-least-32-characters-long
